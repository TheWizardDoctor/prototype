<!DOCTYPE html>
<html>
  <head>
    <title>Prototype</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'custom', 'data-turbolinks-track': 'reload' %>
  </head>
  <%= render 'layouts/header' %>
  <body id="bootstrap-override"> <!-- bootsrap-override is so css can be peronalized and override bootstrap -->
    <%= yield %>
    <%= debug(params) if Rails.env.development? %> <!-- debug box at the bottom -->
  </body>
</html>
