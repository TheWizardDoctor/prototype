yeet
<%= form_with(model: @connector, local: true) do |form| %>
  <% if @connector.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@connector.errors.count, "error") %> prohibited this Team from being saved:</h2>

      <ul>
      <% @connector.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :team_id %>
    <%= form.text_field :team_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
