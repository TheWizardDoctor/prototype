<%= form_with(model: quater, local: true) do |form| %>
  <% if quater.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quater.errors.count, "error") %> prohibited this quater from being saved:</h2>

      <ul>
      <% quater.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
